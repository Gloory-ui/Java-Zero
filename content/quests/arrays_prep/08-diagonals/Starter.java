import java.util.Scanner;

public class ArraysPrep {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int n = sc.nextInt();
        int[][] m = new int[n][n];

        // 1. Заполни матрицу: m[i][j] = i * n + j + 1; и выведи её по строкам
        // 2. Выведи "Главная:" и элементы m[i][i]
        // 3. Выведи "Побочная:" и элементы m[i][n - 1 - i]

    }
}
