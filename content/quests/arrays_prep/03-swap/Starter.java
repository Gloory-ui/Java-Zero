import java.util.Scanner;

public class ArraysPrep {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int n = sc.nextInt();
        int[] a = new int[n];
        for (int i = 0; i < n; i++) {
            a[i] = sc.nextInt();
        }

        // 1. Найди индекс максимума: int maxIndex = 0;
        // 2. Выведи "Индекс максимума: " и maxIndex
        // 3. Поменяй a[0] и a[maxIndex] местами через temp
        // 4. Выведи "После обмена: {...}"

    }
}
