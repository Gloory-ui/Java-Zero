## Ветвление логики через switch

В калькуляторе арифметическую операцию определяют через `switch (op)`. При делении на ноль обязательно проверяй `if (num2 == 0)`!

### Что должна вывести программа

При вводе `3 + 4`:

```text
7.0
```
